<Window x:Class="ApplicationMonitor.WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ApplicationMonitor.WPF"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="90" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <GroupBox Header="Target application" Padding="10" Margin="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="100" />
                </Grid.ColumnDefinitions>
                <TextBox Text="{Binding FilePath}" />
                <Button Grid.Column="1" Content="Browse" Margin="5,0,0,0" Click="OnBrowseClicked" />
            </Grid>
        </GroupBox>
        <StackPanel Grid.Row="1">
            <Button Content="Start" Height="30" Width="100" Click="OnStartClicked" />
            <Button Content="Hide app" Height="30" Width="100" Click="OnMinimizeMonitoredToTrayClicked" />
        </StackPanel>
    </Grid>
    
</Window>
